{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "exactOptionalPropertyTypes": true,
    "declaration": true,
    "outDir": "dist",
    "rootDir": "src",
    "skipLibCheck": true,
    "types": ["node"]
  },
  "//": [
    "rootDir is `src`, and `include` is src ONLY, because tsc roots its output at",
    "the common ancestor of every input. With anything outside src in scope that",
    "ancestor becomes the repo, so the emit lands at dist/src/index.js while",
    "package.json points at dist/index.js. Importing by package name then fails",
    "with 'Cannot find module', and nothing in the build says so — tsc and the",
    "typecheck are both clean.",
    "",
    "That is why test/ is checked by tsconfig.test.json instead of being added",
    "here. `npm run typecheck` runs both: this one to build and check src, then",
    "that one to check the tests against the emitted .d.ts, which is the surface",
    "consumers actually get. Keeping them apart changes what is emitted, not what",
    "is verified.",
    "",
    "lib includes DOM for `crypto.subtle` and `atob`. Both are equally standard in",
    "Node 18+, but their type declarations live in the DOM lib, not in @types/node."
  ],
  "include": ["src/**/*.ts"]
}
