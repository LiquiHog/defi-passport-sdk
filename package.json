{
  "name": "@liquihog/defi-passport-sdk",
  "private": true,
  "version": "0.2.0",
  "description": "TypeScript SDK for LiquiHog DeFi Passport — create and manage a passport.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc && tsc -p tsconfig.test.json",
    "prepare": "tsc",
    "test": "tsc && node --test --experimental-strip-types test/entitlement.test.ts test/gascap.test.ts test/programs.test.ts test/swap.test.ts test/create.test.ts",
    "live-check": "tsc && node scripts/live-check.mjs"
  },
  "dependencies": {
    "algosdk": "3.6.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
